{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "old-dev": "start \"%cd%\" npx tsc index.ts --watch --outDir build && sam local start-api -p 3001 -t dev-template.yaml",
    "dev": "sam local start-api -p 3001 -t dev-template.yaml"
  },
  "dependencies": {
    "node-fetch": "^3.1.0"
  },
  "devDependencies": {
    "typescript": "^4.5.2",
    "webpack": "^5.64.4",
    "ts-loader": "^9.2.6",
    "webpack-cli": "^4.9.1"
  }
}
